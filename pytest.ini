[pytest]
# This registers the option for the pytest-asyncio plugin
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Optional: suppress the warning if you have a global installation
filterwarnings =
    ignore:Unknown config option. asyncio_mode:pytest.PytestConfigWarning